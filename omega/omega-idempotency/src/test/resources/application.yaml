server:
  port: 8088

---
spring:
  profiles: mysql
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/saga?useSSL=false
    platform: mysql
    continue-on-error: true
  jpa:
    properties:
      eclipselink:
        ddl-generation: none